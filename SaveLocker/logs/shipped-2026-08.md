# Shipped — migrated out of Backlog (August 2026)

Backlog items that are now complete, moved here so `Backlog.md` holds only
not-yet-done work. Full technical detail lives in `logs/sessions.md` and the
per-topic write-ups linked below; this is the quick "what shipped, where" index.

| Item | Shipped in | Verified | Detail |
|------|-----------|----------|--------|
| **Linux / Steam Deck agent auto-update** — the update channel (`/api/agent/latest`) was installer-shaped and Windows-only; a Deck user had to re-run `install.sh` by hand. Four phases, one per session: (1) server installer store becomes platform-aware, (2) agent check-only on both platforms, (3) agent stages and applies (download → verify digest → unpack → smoke-test while running, swap runs from the unit's `ExecStartPre` on the *next* start because `systemctl --user stop` kills the whole cgroup a child updater would die in), (4) policy/opt-out (`AutoUpdate: false`), Game Mode + `doctor` surfaces, docs | v0.5.5–v0.5.7 (PR chain, 2026-08-14/15) | All 4 phases individually DONE and suite-verified (`run-linux-tests.sh` 117 → 123/123 by Phase 4; `run-server-bugbounty-tests.ps1` 145 → 164/164 at Phase 1); real Deck update watched end-to-end (staged + applied itself across a reboot, v0.5.7 rollout) | `tasks/LinuxAutoUpdate.md` (executed in place), `logs/2026-08-15_linux-auto-update.md` |
| **Harden the `systemd --user` unit** — folded into Auto-update Phase 3. `NoNewPrivileges`, `PrivateTmp`, `LockPersonality`, `RestrictSUIDSGID`, `UMask`, `PrivateMounts`, address-family allow-list | v0.5.5, 2026-08-15 | `systemd-analyze --user security savelocker.service` on the real Deck: all seven settings confirmed in effect (the raw 8.5 EXPOSED score is a known false read for `--user` units — see [[Gotchas]] → Testing) | [[Backlog]] → *Testing* note; [[Gotchas]] |
| **Linux release provenance** — folded into Auto-update Phase 4. Pinned `release.yml`'s actions to full commit SHAs; SHA-256 checksums + GitHub artifact attestation published for the tarball; draft → attach all assets → publish flow; verification command documented beside the Deck install instructions | Phase 4, 2026-08-15 | `logs/2026-08-15_linux-auto-update.md` Phase 4 outcome | same |
| **v0.5.0 post-release verification — `tests/linux/run-linux-tests.sh` in WSL** (40 checks) | main, 2026-08-08 (PR #36) | 40/40 — had never passed before; `Storage__AgentInstallerRoot` was unset, killing the server after migrations and cascading 16 failures. Fixed in the harness | Backlog (removed 2026-08-20) |
| **"Install update now"** — `/api/agent-version` distinguishes **staged** (safe, offline, instant) from merely **available** (needs a fresh download); `stagedBlockedReason` names the game blocking a restart-triggered swap; Game Mode's dead-end *"Nothing to do."* replaced with a device-probed instruction (Desktop-mode switch only works when systemd lingering is off) | v0.5.8, 2026-08-16 | `run-linux-tests` 208 → 216/216; new checks confirmed to fail against deliberately broken builds; **v0.5.7's unattended self-update and the Game Mode "update is ready" notice both proven on the real Deck for the first time** during the v0.5.7 rollout | `logs/2026-08-15_install-update-now.md` |
| **Decky plugin Phase 5 — CODE only, agent keeps the plugin updated** — server hosts the plugin zip in a third installer slot (`?platform=decky-plugin`); Linux agent replaces `~/homebrew/plugins/SaveLocker`'s files (Decky hot-reloads via the `debug` flag); every destination proven writable before a byte is written, since the plugin dir is root-owned 755 and `plugin.json` is root's outright. **Still in [[Backlog]]**: nothing has run on hardware yet — the plugin zip has never been uploaded to the Decky-plugin installer slot | v0.5.6 (agent) + SaveLocker-Decky v0.2.1 (plugin), 2026-08-15/16 | `run-linux-tests` 161 → 197/197 (harness only — no hardware pass yet) | `logs/2026-08-15_decky-plugin.md` → Phase 5 |
| **Console UI/audit cleanup** — NavBar button sizing + icon-only Refresh; Info-severity agent events (routine auto-updates) no longer read as "problems" (blue "N updates" badge, not red "N problems"); Audit Log gained CSV export plus real detail for package updates, conflict-resolution winners, and exclude-glob diffs; Agent Updates card condensed to status + one Edit modal (bulk fetch, per-package GitHub `SHA256SUMS*.txt` hash verification); auto-fetch interval became a real schedule (disabled/hours/weekly/monthly, server-side next-occurrence computation) | v0.5.9, 2026-08-16 | `run-server-bugbounty-tests.ps1` and console build/lint clean each commit; `AutoFetchScheduler.ComputeNextRun` covered by `tests/run-schedule-tests.ps1` | `logs/2026-08-16_AgentUpdatesRedesign.md`, `2026-08-16_AuditEventTypes.md`, `2026-08-16_AuditLogCsvExport.md`, `2026-08-16_UpdateScheduling.md` |
| **Cyberpunk 2077 uploads (large-save timeout)** — a Cloudflare-proxied server enforces a fixed ~100s cap on any single request; a 100+ MB save archive could never finish in one shot on a normal home upload link. Chunked upload protocol (`/upload/begin` → `/upload/{id}/chunk` → `/upload/{id}/complete}`, ~4 MiB per request, per-chunk retry); old single-shot route kept as an automatic fallback for an agent newer than its server | v0.5.10, 2026-08-18/19 | Reproduced first with a direct `curl` upload (identical 524 at ~100s, ruling out an agent-side bug); fix confirmed on the maintainer's own hardware after a local console + agent test-installer deploy | `logs/2026-08-18_cyberpunk-upload-timeout.md`; **console + agent both redeployed to production, 2026-08-20** |
| **Agent UI live sync activity** — Overview page gained a "Sync now" button, current phase/game with a byte progress bar for a push, and a short rolling activity log; `SyncEngine.SyncAllAsync` dedupes what the tray menu and the button both do; agent UI header now shows the running *build*, not just the version number | v0.5.10, 2026-08-18/19 | End-to-end through a real running tray + isolated console (sync-now click → activity log → stat updates); mid-transfer progress itself could not be screenshotted locally (loopback is too fast) but the same code path executed 20+ times without error during the chunked-upload testing | same |
| **Save-path detection reliability on the real Deck** — four root causes found and fixed across two rounds: MoonDeck-streamed shortcuts never resolved a prefix at all (`LinuxGameScanner` now also tries the AppID named in `MOONDECK_STEAM_APP_ID=`); Steam Cloud's own sync folder needing no Wine prefix was gated on one existing (Left 4 Dead 2); a relocated install (SD card) left a fresh useless prefix while the original stayed in the main root, wrong-cased (Borderlands 2); `<root>` is now always the verified Steam root, never derived from whichever prefix path resolved | main, 2026-08-19 (branch `claude/steam-deck-save-detection-68b6c5`, merged as PR #6 `linux-save-detection-fix`) | `run-linux-tests` 216 → 234 across the session (225/227 pass at the interim count — the 2 failures are pre-existing, unrelated Decky messaging); confirmed on the real Deck for every named game (Cyberpunk 2077, Left 4 Dead 2, Borderlands 2); A Short Hike and Roadwarden confirmed to have no bug (native build / never Proton-launched, respectively) | `logs/2026-08-19_moondeck-save-detection.md` |
| **One game, several real sources — tie-break bug (step 1 of a larger, still-open item)** — a MoonDeck shortcut's synthetic pointer could win a dedupe tie against a genuinely-installed copy of the same game once MoonDeck shortcuts started resolving prefixes at all (the round above); the pointer's `SteamAppId` would then only ever be seen during a MoonDeck session, never a genuine local one | main, 2026-08-19 (same PR #6) | `ScanAsync` now carries a `ViaMoonDeck` flag and sorts it ahead of the `HasSteamCloud` tie-break; pinned by a new `Fake Dual Source Game` fixture, `run-linux-tests.sh` 227 → 230; confirmed no regression on the real Deck across all four session games | `logs/2026-08-19_moondeck-save-detection.md`; **steps 2–4 of this item remain open in [[Backlog]]** |
| **`doctor` names an unmounted-but-configured Steam library** — Cyberpunk 2077 was reported wrong a second time; turned out the Deck's SD card holding its real 91 GB Steam install simply wasn't inserted, and no scanner (ours or Steam's) can read missing media — what was missing was saying so | main, 2026-08-19 (same PR #6) | `run-linux-tests` 230 → 232 | `logs/2026-08-19_moondeck-save-detection.md` |
| **A MoonDeck shortcut's real AppID reclassifies as `SteamInstalled` when Steam's own `libraryfolders.vdf` `apps` block says it's installed** — closes the same Cyberpunk case with no card swap needed, using a signal (`SteamRoots.AllKnownInstalledAppIds`) that survives the card's own media being absent | main, 2026-08-19 (same PR #6) | `run-linux-tests` 232 → 234; confirmed on the real Deck with the SD card still not inserted | `logs/2026-08-19_moondeck-save-detection.md` |
| **Case-insensitive path matching inside a Wine/Proton prefix** — `PathResolver.Wine()`/`.Proton()` now fall back to a segment-by-segment, case-insensitive walk against the real directory listing when the naive exact-case path doesn't resolve (generalizes the Borderlands 2 hand-fix above to any future mis-cased prefix); newest-mtime wins on an ambiguous sibling match | main, 2026-08-20 (branch `claude/wine-proton-case-insensitive-paths-815ba3`, merged as PR #8) | `run-linux-tests` 234 → 237 (235 pass; 2 pre-existing unrelated Decky-messaging failures); confirmed by hand against both new fixtures with `savelocker resolve --prefix` | `logs/2026-08-20_wine-case-insensitive-and-scoping.md` |
| **testenv: Steam Deck build/deploy target, and a real `clean`** — `tests/testenv.ps1 build -Only deck` cross-publishes a self-contained tarball and `up` deploys it beside the installed release agent in an isolated state dir (`~/savelocker-test` / `~/savelocker-test-state`), never touching the real install; `clean` tears down every target including the dashboard container's image and volume, which a plain `down` left behind | main, 2026-08-17–19 | All four commands (`build`/`up`/`down`/`clean`) verified end-to-end against the real Deck, the WSL clone, and Docker; the installed Deck agent's pid confirmed unchanged throughout. Found and fixed three real bugs on the first hardware run: a fresh worktree's `agent-ui` needing `npm install`, a token-read race in `cmd_up`, and a `setsid`/`disown` daemon that does NOT survive its SSH session closing on a real systemd Deck (fixed with `systemd-run --user --scope`) | [[Gotchas]] → *`tests/testenv.ps1`*; [[Build and Run]] → *Throwaway test rig* |
| **Add Games: filter by save-path detection, and a Game Mode Activity card** — replaced the single-select "Needs path" filter (couldn't combine with a source filter) with a "Path: All / Detected / Not detected" row that stacks with Suggested/Steam/Heroic filters, in both the browser agent UI and Game Mode; Game Mode's Overview screen gained the browser UI's Activity card via a new durable `SyncActivityStore` (the daemon and `savelocker ui` are separate OS processes); fixed three font-rendering gaps (em dash, ellipsis, curly quotes missing from the embedded font) | main, 2026-08-20 (PR #9) | Build/lint clean; `SyncActivityStore` follows the same durable-file pattern as `LeaseWarningStore` | commit `9a0ec4b` |
| **Markdown tables render in the console** — `HelpView.tsx`/`WhatsNewView.tsx` were bare `<ReactMarkdown>`, so a GFM table rendered as a wall of raw pipe characters; `cli-reference.md` (almost entirely tables) was the worst-hit page. Added `remark-gfm`, plus `rehype-raw` so the `<br>`-separated multi-line option lists inside `cli-reference.md`'s table cells render as real line breaks instead of literal `<br>` text (react-markdown drops raw HTML by default). Table CSS made `display: block; overflow-x: auto` so a table wider than the content pane scrolls sideways instead of being silently clipped by the console's fixed-height, overflow-hidden root (the bounded-flex-column trap in [[Gotchas]]) | 2026-08-23, no task file | `npm run build` (tsc + vite) clean; verified live in the dev server — all 5 `cli-reference.md` tables render with working `<br>` line breaks, no horizontal page overflow (`bodyScrollWidth === bodyClientWidth`) | commit `6ec2727` |

## Rollout confirmations
- **v0.5.8 (console + Windows/Linux agent installers) and the Cyberpunk chunked-upload fix: redeployed to production, 2026-08-20** (maintainer-confirmed). This closes CONTEXT.md's former Next action items 0 and 1.
- **`claude/steam-deck-save-detection-68b6c5` and `claude/wine-proton-case-insensitive-paths-815ba3`: both merged to `main`** (PRs #6 and #8) — closes CONTEXT.md's former Next action items 4 and 5.
