# Shipped — migrated out of Backlog (September 2026)

Backlog items that are now complete, moved here so `Backlog.md` holds only
not-yet-done work. Same convention as `shipped-2026-07.md` / `shipped-2026-08.md`.

| Item | Shipped in | Verified | Detail |
|------|-----------|----------|--------|
| **Self-host the console fonts** — the dashboard loaded Inter + JetBrains Mono from Google Fonts at runtime, so an offline-LAN console fell back to system fonts. Now vendored via Fontsource (`@fontsource/inter` 300–700, `@fontsource/jetbrains-mono` 400/500); no `googleapis`/`gstatic` reference left in `web/` | branch `easy-wins`, 2026-09-08 (`5ffd7d1`), not yet merged | `web` build + `oxlint` clean; DevTools Network shows zero Google requests, and blocking `*googleapis*`/`*gstatic*` renders byte-identical UI. Open follow-up, not part of this item: `agent-ui/index.html` still loads Inter from Google Fonts | `logs/2026-09-08_self-hosted-fonts/` |
| **Fresh installer enrollment — ACL trap (WA-03)** — the elevated installer deliberately never creates `%PROGRAMDATA%\SaveLocker`; the agent creates it on first write and `StateDirSecurity.Protect` breaks inheritance, strips `Authenticated Users`/`Users`/`Everyone`, keeps self + SYSTEM + Administrators. No code change — verification only | verified 2026-09-08, no code change | Live `icacls` on an installed box (SYSTEM/Administrators/self full, inheritance broken); WA-03 section of `tests/run-winagent-tests.ps1` asserts the same on a scratch dir. A literal second-login attempt remains unexercised (the suite asserts the ACL that decides it) | `logs/2026-09-08_fresh-installer-enrollment/` |
| **LAN enrollment URL** — minting an enrollment file while browsing the console at `localhost` used to bake an unreachable `http://localhost:5080` into the policy. Inferred loopback is now refused before burning the single-use token (HTTP 400 naming the fix); `GET /api/admin/enrollments/effective-url` previews the URL pre-mint. No code change — verification only | verified 2026-09-08, no code change | `effective-url` returns `isLoopback:true` on localhost / `false` with the LAN host on `192.168.0.124:5196`; localhost mint without explicit URL → 400, explicit LAN URL → 200 with that URL in the policy. End-to-end redeem from a second machine not exercised | `logs/2026-09-08_post-release-verification/` |
| **One game, several sources — cross-source doctor note (step 2)** — `LinuxGameScanner.ScanUnfilteredAsync` + `PickWinner`, `ScanCandidate.MoonDeckAppId`, and `Doctor.ReportCrossSourceDuplicatesAsync`: one line per title found in 2+ places, every origin named, `[tracked]` on the copy the scan picked, plus the `savelocker add-game` hint for switching. A note, never a problem — exit code unaffected | branch `easy-wins`, 2026-09-08 (`bc82708`), not yet merged | `run-linux-tests.sh` dual-source checks green, including two new doctor assertions (`found in two places`, MoonDeck origin named); full suite green as root on ext4 (the `chattr +i` tamper probe needs `CAP_LINUX_IMMUTABLE` — exit 2 unprivileged is environmental, not a failure) | `logs/2026-09-08_one-game-several-sources/` |
| **Game Mode stale game list** — `savelocker ui` loaded `config.json` once, so a server-side deletion ghosted until restart. New `AgentConfig.RefreshGameList` (membership-only: adopt/drop by `GameId`, surviving entries untouched) polled from `UiApp` every 10s including the first frame; lock contention defers to the next poll, unreadable file is a no-op | branch `easy-wins`, 2026-09-08 (`d6d4814`), not yet merged | Live under WSLg: added game appears and deleted game disappears within ~15s, no restart. Deliberately out of scope: renames don't propagate (same IDs → no-op, by design); a malformed `GameId` crashes `Load` at startup yet is silently skipped by the poll's `ReadOnDisk` — robustness gap worth its own item | `logs/2026-09-08_game-mode-stale-list/` |
